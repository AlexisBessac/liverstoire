<form method="post" action="{{ path('app_admin_periods_delete', {'id': period.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette période ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ period.id) }}">
    <button class="btn btn-danger" title="Supprimer">Supprimer</button>
</form>
