{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary" title="{{ button_title|default('Ajouter') }}">
        {{ button_label|default('Ajouter') }}
    </button>
{{ form_end(form) }}
