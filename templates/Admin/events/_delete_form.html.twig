<form method="post" action="{{ path('app_admin_events_delete', {'id': event.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cet évènement historique ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <button class="btn btn-danger" title="Supprimer">Supprimer</button>
</form>
